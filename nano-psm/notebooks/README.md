# Notebooks

Colab notebooks for resumable Nano PSM training belong here.

The first notebook should:

- install Python dependencies
- download generated datasets from Hugging Face Hub
- download the latest checkpoint snapshot
- run `src/nano_psm/train.py` with `--resume auto`
- upload checkpoints and metrics back to Hugging Face Hub

